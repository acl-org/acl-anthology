<div class="row">
  <%- model_class = Venue -%>
  <div class="span12 page-header">
    <h2><%= @venue.name %> (<%= @venue.acronym %>)</h2>
  </div>

  <!-- <div class="span2">
    <%- event_class = Event -%>
    <h3 style="text-align:center"><%=t '.title', :default => event_class.model_name.human.pluralize.titleize %></h3>
    <table class="table table-striped">
      <tbody>
        <% @events.sort.reverse.each do |event| %>
          <tr>
            <td><%= link_to event.year, event_path(event) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <div id="intro" class="span10">
    <% intro_path = "app/views/venues/intros/_" + @venue.acronym + ".html.erb" %>
    <% if File.exist?(intro_path) %>
      <% intro_partial = File.open(intro_path,'r') %>
      <%= render "venues/intros/" + @venue.acronym.downcase %>
    <% end %>

  </div> -->

  <div class="span12">
    <% @events.each do |event| %>
      <h4 style="text-align:center"><%= link_to @venue.acronym + " " + event.year.to_s, event_path(event) %></h4>
      <% @volumes = event.volumes %>
      <table class="table table-striped table-bordered">
        <thead>
          <tr>
            <th>Anthology</th>
            <th>Volume</th>
            <th>Papers</th>
          </tr>
        </thead>
        <tbody>
          <% @volumes.each do |volume| %>
            <tr>
              <td><%= volume.anthology_id %></td>
              <td><%= link_to volume.title, volume_path(volume) %></td>
              <td><%= volume.papers.size %></td>
            </tr>
          <% end # of @volumes.each block %>
        </tbody>
      </table>
      <br />
    <% end # of @events.each block %>
  </div>

</div>