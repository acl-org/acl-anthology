# 2026.semeval-1: issue 9367 audit and repairs

Audit and local repairs: 2026-09-23. Prepared by GitHub Copilot.

## Scope and outcome

Worked through the correction checklist in
[issue #9367](https://github.com/acl-org/acl-anthology/issues/9367) against current
metadata, published PDFs, existing revisions, issue attachments, and organizer
source material. This is a verification and repair of that checklist, not a new
exhaustive audit of all 455 papers.

**Ready locally:** four replacement PDFs; corrections to 18 existing paper records;
an updated person ID and additional name variant for an existing verified person.
Changes comprise
12 titles, four author lists, four abstracts, two page ranges, four PDF checksums,
and six regenerated citation keys. All paper IDs, DOIs, existing revisions,
and unrelated paper records are preserved. No volume re-ingestion was performed.

Re-ingestion is neither necessary nor advisable: the supplied source still contains
metadata errors and a duplicate PDF. In-place repairs preserve existing citations
and revisions without an exception to the assigned-paper-ID policy.

**Still blocked:** anonymized paper `.259` and the mismatched PDF for omitted
source record `280`. Organizer correspondence explains source record `483` as
obsolete metadata for the existing PsyCoMark overview, not another missing paper.
Do not consider issue #9367 fully resolved until the two remaining blockers are settled.

Files are staged in `~/anthology-files/pdf/semeval/`. **No `make sync` or PDF upload
was performed.** The metadata corrections and this report are prepared for a PR;
issue #9367 must remain open for the outstanding organizer-dependent items.

## PDF checklist

All abbreviated IDs below have prefix `2026.semeval-1`.

| ID | Finding and action | Status |
| --- | --- | --- |
| `.59` | Published PDF contained UIT-AMMC. Recovered HCMUS RepeatedGames from organizer source `67.pdf`, the paper also preserved at `.60v1`; added correct footer and pages 409-416. | Staged |
| `.60` | Current canonical PDF and `.60v2` exactly match the attachment in issue #9118. Retained the correction and both revision records. | Already fixed |
| `.95` | Published PDF contained ChulaNLP, the paper at `.341`. Recovered the seven-page HCMUS DroneBoys PDF from current organizer source `104.pdf`. | Staged |
| `.142` | Published PDF contained the Task 9 overview at `.453`. Recovered six-page king001 from current organizer source `159.pdf`; added pages 1032-1037. | Staged |
| `.259` | Both published PDF and latest organizer source `292.pdf` still say "Anonymous ACL submission" and include review line numbers. | Needs camera-ready PDF |
| `.278` | Published PDF was the unrelated HU SemEval-2025 Task 9 paper. Recovered the eleven-page HUS@NLP-VNU SemEval-2026 Task 3 PDF from current organizer source `312.pdf`. | Staged |
| `.416` | Current PDF exactly matches the non-anonymous attachment in issue #9167, which was already resolved. Left unchanged. | Already fixed |

The former duplicates at `.95`/`.341` and `.142`/`.453` are duplicate paper content,
not identical current file hashes: their proceedings footers differ. The staged
replacements contain the intended papers and no longer duplicate those controls.

### Staged files and pagination

| Canonical PDF | Old CRC32 | New CRC32 | PDF pages | Metadata pages | Pagination |
| --- | --- | --- | --- | --- | --- |
| `2026.semeval-1.59.pdf` | `148c190c` | `46403a0b` | 8 | 409-416 | Added printed pages; shortened old 409-417 allocation |
| `2026.semeval-1.95.pdf` | `4e7e53d4` | `780a3d0c` | 7 | 658-663 | Unpaginated source; retained published allocation |
| `2026.semeval-1.142.pdf` | `62fa4d53` | `726e202c` | 6 | 1032-1037 | Added printed pages; shortened old 1032-1049 allocation |
| `2026.semeval-1.278.pdf` | `5202ba57` | `c142375e` | 11 | 2200-2208 | Unpaginated source; retained published allocation |

The user explicitly chose to preserve published citation ranges for the two longer
replacements, `.95` and `.278`, and stage their unpaginated camera-ready originals.
Their metadata ranges therefore describe the original allocation, not the current
PDF length. Extending them would overlap `.96` or `.279`; no neighboring papers
were renumbered. The shorter replacements leave unused pages 417 and 1038-1049.

Existing corrected `.60` and `.416` likewise have PDF lengths differing from their
historical metadata allocations: nine versus eight pages and sixteen versus fourteen,
respectively. Those existing fixes and ranges were preserved, not overwritten.

## Metadata checklist

| ID | Completed correction |
| --- | --- |
| `.9` | Changed the publication name to Juan David Villate; preserved the user's person-ID rename from `juan-villate-lemus` to `juan-david-villate`, verified the matching paper reference, and retained ORCID `0009-0007-5777-8808` and both name variants. |
| `.39` | Title now ends "Detecting Machine-Generated Code by Using Different Transformer Models". |
| `.110` | Restored "Multi-Style Humor Generation via Comedian-Inspired Prompting and LLM Self-Assessment"; inserted Michael Olaolu Arowolo between Utsav Arora and Andrew Hoblitzell. |
| `.115` | Restored the full Team JCT 2026 / AmbiStory title from the PDF, without inventing punctuation at its line break. |
| `.145` | Restored "ELSER-Based Sparse Retrieval with Mistral-7B for Multi-Turn RAG Evaluation". |
| `.175` | Restored "Query Diversity via Nested Reciprocal Rank Fusion and Evidence-Guided Agentic Generation for Multi-Turn RAG". |
| `.218` | Restored "A Locale-Conditional, Multi-Model Prompting Pipeline for Hausa Cultural Question Answering". |
| `.249` | Replaced unrelated authors with Younghee Jeong, Yixin Zhao, and Hoang Bao Trung Le in PDF order; replaced the copied abstract with the PDF abstract. Also addresses issue #9190's metadata request. |
| `.333` | Restored "Multi-Label Polarization and Manifestation Detection in Urdu". |
| `.336` | Restored "Dense Transformer Networks for Narrative Story Similarity and Representation". |
| `.342` | Restored "Emotion-Aware Multi-Task Learning for Conspiracy Detection". |
| `.388` | Restored "Team p1" and the PDF's "Semantic Bridge - Augmented Encoding for Word Sense Plausibility" wording. |
| `.424` | Restored "Mean Pooling and Narrative Density for Conspiracy Belief Detection". |
| `.434` | Replaced unrelated authors with Vivek Dhayal and restored the PDF abstract. |
| `.142` | Matched "Q&A" in the recovered PDF title and restored its abstract's two citations. |
| `.278` | Restored the recovered PDF abstract's final code-availability sentence. |

Title edits use the repository's fixed-case protection. Citation keys were refreshed
for changed title/author metadata; six actually changed. The `.249` correction did
not reuse the unrelated person IDs supplied in the issue's correction form.

## Omitted source records

- **Source record `280`:** the original metadata describes "Ping An at SemEval-2026
  Task 9: Data Augmentation, Multitask Fine-tuning, and Model Fusion for Multilingual
  Polarization", by Junxin Lin, Zhichao Meng, and Lianxin Jiang. It was marked
  accepted but omitted from final `papers.yml`. The latest repository now includes
  `280.pdf`, but that file instead contains "PingAn-NLP at SemEval-2026 Task 9:
  Multi-Stage Alignment via GRPO and Tiered Ensemble Voting for Multilingual
  Polarization Detection", by DiYang Chen and YouZhen Pang. The email attachment
  supplies the same file. Full-text comparison with existing `.354` (source record
  `392`) finds only the added proceedings footer and page numbers 2810-2816.
  The attached YAML still lists the different title and three authors for record
  `280`, and separately lists the two-author paper as record `392`. Do not create
  a new record from this duplicate. Obtain the intended PDF or organizer confirmation
  that record `280` should also be withdrawn as obsolete.
- **Source record `483`:** original metadata describes "SemEval-2026 Task 10:
  Conspiracy Detection", by David Rodriguez and Mario Graff, marked accepted.
  Kai's attached change list explicitly identifies this as an old title for the
  PsyCoMark overview and says the unnecessary record was removed. The attached
  YAML indeed omits `483` and retains the overview as source record `497`, already
  published as `.444`. Treat this as resolved organizer clarification: no new paper
  or PDF is needed. The earlier request for a missing `483.pdf` is superseded.

No new IDs, DOIs, or placeholder papers were created. Source submission numbers
`280` and `483` must not be confused with Anthology paper-number suffixes.

## Email attachment cross-check

Reviewed `2026_proceedings_fixes.docx` and all files in
`2026_proceedings_fixes_files.zip`, supplied under
`/Users/mattpost/Downloads/semeval/`, on 2026-09-23.

| Attached PDF | CRC32 | Verified relationship |
| --- | --- | --- |
| `66.pdf` | `a25da84c` | Byte-identical to the RepeatedGames source used for `.59`; our staged version adds its footer and page numbers. The earlier source copy was named `67.pdf`. |
| `104.pdf` | `780a3d0c` | Byte-identical to staged `.95`. The document also calls RepeatedGames `104.pdf`; the actual attachment establishes that this is DroneBoys. |
| `159.pdf` | `704e3123` | Byte-identical to the king001 source used for `.142`; our staged version adds its footer and page numbers. |
| `280.pdf` | `5c3a1307` | Byte-identical to the previously inspected source; duplicates published `.354` apart from proceedings footer/page numbers. |
| `312.pdf` | `c142375e` | Byte-identical to staged `.278`. |

The attached `original_papers.yml` is structurally identical to the file at the
pinned organizer commit below: 456 unique source records. It does not supersede
the in-place metadata repairs. For example, source `484` (Anthology `.434`) still
lists David Rodriguez and Mario Graff instead of Vivek Dhayal, and source `122`
(`.110`) still has the old BAHAHA title and omits Michael Olaolu Arowolo. Source
`281` (`.249`) has the right three names but still differs from PDF author order.
Uploading PDFs or removing source `483` does not itself repair those fields in
the already-published Anthology records.

The document explicitly says `.259`'s authors never submitted the camera-ready
and have been contacted. The ZIP contains no `292.pdf`; this remains unresolved.
The four staged replacements are supported by the attachments and were not changed
by this follow-up check. No authoritative metadata was changed during the check.

## Additional author questions

These differences were observed while checking the requested titles but were not
silently resolved as part of the checklist:

- `.145`: metadata orders Soumendra Ray before Garima Gupta; PDF orders Garima
  Gupta before Soumendra Nath Ray.
- `.218`: metadata lists five authors; PDF lists four, omits Aliyu Rabiu Shuaibu,
  orders Sani Aji before Lukman Jibril Aliyu, and uses expanded names for Faisal
  Muhammad Adam and Lukman Jibril Aliyu. Confirm authorship before deleting anyone.
- `.115` and `.388`: PDF names include Batel Sara Turjeman and Pawan Kumar Rajpoot,
  respectively, where metadata uses shorter names.
- Recovered `.59`, `.95`, and `.278` use fuller Vietnamese name forms than submitted
  bibliographic metadata. The organizer source IDs, author order, and available
  submission affiliations/email evidence support the paper identities; existing
  name specifications were retained. Do not treat this as exact spelling agreement.

## Evidence and validation

- Anthology baseline commit: `e4d087a11bc06b4600281bba45c40f3ab7a41bbe`.
- Organizer repository: `aclpub2026/ws_semeval`, pinned commit
  `eb9a4503cdd7e7c4dbde3a0ff81236bbfe6b6a2c`, retrieved through Git. The existing
  local ingestion checkout was not modified. Its old `312.pdf` was still incorrect.
- Evidence directory: `/tmp/semeval-audit-20260923/`. Includes the pre-repair metadata
  snapshot of all 456 records (455 papers plus front matter), downloaded PDFs and
  extracts, issue attachments, pinned source PDFs, staging manifests, and repair scripts.
- Checked 23 checklist/control current PDFs, the two `.60` revision PDFs, both linked
  author attachments, and the additional `.354` control. Download hashes matched
  the stored checksums; wrong content can still have a valid stored checksum.
- Verified complete extracted content and every added page number for `.59` and
  `.142`. `.95` and `.278` are byte-for-byte copies of their verified source PDFs.
- All four staged PDFs are non-anonymous; their first pages were rendered and
  visually inspected. All four abstracts agree with source text after removing
  PDF-only footnote markers and separate GitHub link labels.
- XML changes were made only through public `acl_anthology` APIs. Verified saved
  checksums, author corrections, ID/DOI/revision preservation, and schema validity.
- `UV_FROZEN=true make check`: 126 script tests passed, with 12 existing person-index
  warnings. Its pre-commit stage cannot install the isolated hook dependency
  `uv==0.12.17` from the approved package feed, despite that version being installed
  through Homebrew. Validation was completed separately with the installed binary:
  `uv lock --check --offline --no-config` passed, and
  `SKIP=uv-lock uv run --frozen pre-commit run --all-files` passed every other hook.
  Only the isolated uv hook is skipped for the commit; its lock check was run
  explicitly. No hook configuration, package/feed settings, or lockfile were changed.

## Handoff

1. Run `make sync` to upload the four staged PDFs. It removes local source files
   after successful transfer. Publish the metadata changes through the normal workflow.
2. Request a non-anonymous `.259` camera-ready PDF and clarification/a correct file
  for source record `280`. Source `483` is resolved as obsolete; do not request a
  new PDF for it. Keep issue #9367 open for the two remaining blockers.
3. Review the additional authorship questions separately, preserving legitimate
  post-ingestion corrections. The supplied bundle does not settle those questions;
  no unconfirmed author additions, removals, or name-split changes were made.
